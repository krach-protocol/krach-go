package krach

/* type halfconn struct {
	cs   *cipherState
	conn *Conn
}

func (h *halfconn) write(streamID uint8, buf []byte) error {
	if len(buf) == 0 {
		return nil
	}

	if len(buf) > h.conn.maxFramePayloadLength {
		panic("Stream tried to write too much data in one frame")
	}

}*/
